import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-label-floating-change-detection',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
